PORT=3000

# ConvertKit API Credentials
# Replace these with your actual keys in a local .env file (do not commit .env to version control)
CONVERTKIT_API_KEY=your_public_api_key_here
CONVERTKIT_FORM_ID=your_convertkit_form_id_here
